import os, sys, pathlib
_root = os.environ.get("OOM")
if not _root:
    _root = pathlib.Path(__file__).resolve().parents[6]
else:
    _root = pathlib.Path(_root)
_paths = [
    _root / "src",
    _root / "oom-core",
]
for _p in _paths:
    if _p.exists():
        sys.path.insert(0, str(_p))
